# Copy to .env.local and paste the team key from team chat (never commit real keys)
ANTHROPIC_API_KEY=sk-ant-api03-REPLACE_ME
# Engine shared secret — real value in team chat / Vercel env
HERMES_SECRET=REPLACE_ME
# DATABASE_URL is auto-injected by the Vercel Neon integration in deploys;
# for local dev run `vercel env pull .env.local` or use local Postgres
